package com.alcedo.studio.service

import android.graphics.Bitmap

class RenderService {

    fun renderThumbnail(imagePath: String, maxWidth: Int, maxHeight: Int): Bitmap? = null

    fun renderPreview(imagePath: String, maxWidth: Int, maxHeight: Int): Bitmap? = null

    fun shutdown() {}
}
